parameter passing semantic

- primitive value / immutable reference value -> automatic backtrack
  - don't use postfix, and prefix operator on number
- mutable reference value -> manually rewind

backtrack is:

after calling the function recursively with small parameter

using the original parameter